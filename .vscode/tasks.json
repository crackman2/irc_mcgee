{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Release Build",
            "command": "nim c --outdir:./update -d:release --stackTrace:off --app:console --opt:size --checks:off irc_mcgee.nim; strip -s irc_mcgee.exe; upx irc_mcgee.exe --ultra-brute",
            "type": "shell",
            "args": [],
            "problemMatcher": [
                "$tsc"
            ],
            "presentation": {
                "reveal": "always"
            },
            "group": "build"
        }
    ]
}