{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Release Build",
            "command": "nim c -d:release --stackTrace:off --app:gui --opt:size --checks:off irc_mcgee.nim; strip -s irc_mcgee.exe; upx irc_mcgee.exe --ultra-brute",
            "type": "shell",
            "args": [],
            "problemMatcher": [
                "$tsc"
            ],
            "presentation": {
                "reveal": "always"
            },
            "group": "build"
        }
    ]
}